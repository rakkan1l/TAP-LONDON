name: One-time image resolution fix

on:
  workflow_dispatch: {}

jobs:
  fix-images:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install firebase-admin
        run: npm install firebase-admin
      - name: Run gallery check
        env:
          FIREBASE_SERVICE_ACCOUNT_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}
        run: node scripts/check-gallery-pattern.js
      - name: Commit results file
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add gallery-check-results.txt
          git commit -m "Add gallery check results" || echo "nothing to commit"
          git push
